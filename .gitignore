.DS_Store
*.bak
.index.copy.bak
node_modules/

.playwright-mcp/

# Raw and encoded film. Hundreds of megabytes of source video do not belong in
# git — the mockup references assets/video/hero_large.mp4 locally.
HERO VIDEO/
patton old files videos/
# The encoded hero film IS committed — 25MB, and without it the GitHub Pages
# preview opens on a black hero. Everything else under video/ stays out.
mockup/assets/video/*
!mockup/assets/video/hero_large.mp4

# A copy of the installed aan-dealer-mockups skill, not project source.
mockup/aan-dealer-mockups/

# Working screenshots and probes.
mockup/.h3.png
mockup/.probe.html

# Local settings, a working screenshot, and a nested clone of design_dna that
# must not travel — the project deliberately has no local copy, the canonical
# path is always current, and a stale copy that looks authoritative is worse
# than none. Flagged by another session before `git add -A` could sweep it in.
.claude/
index3-before.png
design_dna/
